% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{MixtureMixture.train}
\alias{MixtureMixture.train}
\title{MixtureMixture_train}
\usage{
MixtureMixture.train(X_train, y_train, number_clusters, n_interaction, epislon)
}
\arguments{
\item{X_train}{: matrix of data of dimension individus x feature}

\item{y_train}{: vector of class of dimension individus x feature}

\item{number_clusters}{: max number of cluster to find model inside class}

\item{n_interaction}{: number of gaussian model create to find max likelihood}

\item{epislon}{: difference between two likelihood to stop the model}
}
\value{
list_model: returns the gaussian model to each class
}
\description{
Train a classification model by picking the model parameters
that give as output a highest accuracy and a number of cluster with
the smallest BIC value
}
\examples{
rand<- sample(1:150, 150)
X_train <- iris[rand[1:100], -5]
y_train <- iris[rand[1:100], 5]
y_train <- as.numeric(factor(y_train))
X_test <- iris[rand[101:150], -5]
y_test <- iris[rand[101:150], 5]
y_test <- as.numeric(factor(y_test))
epislon <- 10^-6
number_clusters <- 1:3
n_interaction <- 50
results <- MixtureMixture.train (X_train, y_train, number_clusters, n_interaction, epislon)

}
