% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{MixtureMixture.predict}
\alias{MixtureMixture.predict}
\title{MixtureMixture_predict}
\usage{
MixtureMixture.predict(X_test, n_classes, map_cluster_class, best_models)
}
\arguments{
\item{X_test}{: matrix of data of dimension individus x feature}

\item{n_classes}{: number of class to predict}

\item{map_cluster_class}{: maping of the cluster and its corresponding class}

\item{best_models}{: result of MixtureMixture_train}
}
\value{
y_pred: prediction to each individuals
}
\description{
predict using the classification model
}
\examples{
rand<- sample(1:150, 150)
X_train <- iris[rand[1:100], -5]
y_train <- iris[rand[1:100], 5]
y_train <- as.numeric(factor(y_train))
X_test <- iris[rand[101:150], -5]
y_test <- iris[rand[101:150], 5]
y_test <- as.numeric(factor(y_test))
epislon <- 10^-6
number_clusters <- 1:3
n_interaction <- 50
results <- MixtureMixture.train (X_train, y_train, number_clusters, n_interaction, epislon)
n_classes <- length(unique(y_train))
y_pred <- MixtureMixture.predict(X_test, n_classes, results$map_cluster_class, results$best_models)
}
